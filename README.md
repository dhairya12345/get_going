this project is get going system there are two diffrent module within it customer and driver
to enable firebase otp you create folder name firebase and past the adminSDK file with in it 
add email and password inside settings.py for use email notification
